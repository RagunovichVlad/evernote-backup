require "spec_helper"

describe Evernote::Backup do
  it "has a version number" do
    expect(Evernote::Backup::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
